# AVA 당직 서비스 핸드오프

최종 갱신: 2026-09-13

프로젝트 전체의 지식 DB, wiki/topic 발효 상태, MCP 매뉴얼 및 운영 절차에 관한 주 핸드오프는
[`../ai 당직/HANDOFF.md`](../ai%20당직/HANDOFF.md)이다. 이 문서는 AVA 저장소에 직접 관련된 변경과
남은 장애만 기록한다.

## 현재 반영 내용

- AVA의 로컬 당직 MCP(`server_id=danjik`) 호출에는 호스트가 소유하는 `_ava_context`가 자동으로 붙는다.
  현재 통화의 `call_id`와 `caller_number`를 사용하며, 모델이 같은 필드를 인자로 만들어도 폐기한 뒤 실제 값으로 덮어쓴다.
- 다른 MCP 서버에는 `_ava_context`를 전달하지 않는다.
- 당직 에이전트 안내에서 전화번호 키패드 입력 절차와 `collect_phone_keypad` 노출을 제거했다.
- 연락처가 필요한 접수는 AVA가 자동 수집한 발신번호를 사용하도록 안내한다. 발신번호가 없거나 사용할 수 없으면
  임의의 번호를 만들거나 말로 받은 번호로 대체하지 않고 당직자에게 연결한다.
- AVA 이미지는 로컬에서 재빌드·재시작했고 런타임이 정상 기동하는 것까지 확인했다.
- 위 런타임 확인은 당시 로컬 작업 브랜치를 기준으로 한 것이다. 이후 포크의 최신 `main` 변경과 이 작업을
  통합한 소스는 이 핸드오프 시점에 다시 배포하거나 재시작하지 않았다. 최신 통합 소스가 현재 실행 중이라고 간주하지 않는다.
- 로컬 런타임의 `.env` `AI_ROLE` 설정과 `data/operator/agents.db`도 실행에 맞게 동기화했지만,
  환경 파일·운영 DB·백업은 이 저장소 커밋에 포함하지 않는다.

## 현재 차단 사항

`send_location_link`를 통한 위치 요청 문자가 실제 전화에서 전송되지 않는다. 이 때문에 위치 수신을 포함한
대화 시나리오의 종단 간 테스트를 완료하지 못했다. AVA 재빌드와 정상 기동은 확인했지만, 실제 SMS 전송 성공이나
발신번호 기반 위치 수집 성공을 확인한 상태가 아니다.

다음 작업은 실제 통화 시각의 AVA 도구 실행 로그와 당직 MCP 로그를 함께 확인해
`send_location_link` 호출 여부, `_ava_context.caller_number` 수신 여부, SMS 제공자 응답을 순서대로 추적하는 것이다.
이 장애가 해소되고 실제 위치 문자가 도착하기 전에는 위치 수집 대화 테스트를 완료로 표시하지 않는다.

## 검증 범위

- `tests/test_mcp_tool_context.py`: 당직 MCP에 실제 통화 메타데이터가 주입되고 모델 제공 값은 무시되는지 검증한다.
- 같은 테스트에서 다른 MCP 서버로 내부 통화 메타데이터가 유출되지 않는지 검증한다.
- 운영 DB와 `.env`는 커밋 대상이 아니며, 새 환경에서는 별도의 운영 설정 동기화가 필요하다.
